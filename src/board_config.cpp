/*
 * Board Configuration - Global variable definition
 */

#include "board_config.h"

// Global board type variable (set once during detection in initBoardConfig())
BoardType g_board_type = BOARD_UNKNOWN;
